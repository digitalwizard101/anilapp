import React from 'react'

export const Chamber = ({dada}) => {
  return (
    <div>


        <h1>

            Chamber Component
        </h1>

        <button onClick={dada}> 

            Click Me
        </button>
    </div>
  )
}